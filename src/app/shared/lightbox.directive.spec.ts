import { LightboxDirective } from './lightbox.directive';

describe('LightboxDirective', () => {
  it('should create an instance', () => {
    const directive = new LightboxDirective();
    expect(directive).toBeTruthy();
  });
});
